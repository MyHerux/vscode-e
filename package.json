{
    "name": "x",
    "displayName": "x",
    "description": "x",
    "version": "0.0.1",
    "publisher": "x",
    "engines": {
        "vscode": "^1.15.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "*"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "commands": [
            {
                "command": "pomodoro.start",
                "title": "Pomodoro: Start a pomodoro"
            },
            {
                "command": "pomodoro.cancel",
                "title": "Pomodoro: Cancel pomodoro"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "@types/mocha": "^2.2.32",
        "@types/node": "^6.0.40",
        "mocha": "^5.2.0",
        "typescript": "^2.0.3",
        "vscode": "^1.1.18"
    }
}
